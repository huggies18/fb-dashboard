# Memory - 2026-05-29

- Modified real-time scraper logging in `scrape_all_posts.py` (both under `highsolar/scraper/` and `c1/scraper/`) to print each found post to stdout in real-time, prefixed with a Thai timezone (UTC+7) timestamp.
- Modified the AI parallel filter (`parallel_filter.py` in both `highsolar` and `c1`) progress output to prepend the Thai timezone timestamp to each analyzed post log line.
- Added time tags (`⏱️ ดึงเมื่อ` for fetch time and `🕒 โพสต์เมื่อ` for post age) to each post card header in the admin dashboard page (`dashboard/app/page.tsx`).
- Changed system offset calculations from using `datetime.now()` to timezone-aware `datetime.now(timezone.utc)` (instead of deprecated `datetime.utcnow()`) across all scraper python scripts (`scrape_all_posts.py`, `run_workflow.py`, `parallel_filter.py`) to prevent deprecation warnings and double offsets.
- Created `proxy_config.json` containing the DataImpulse HTTP proxy configuration for both `highsolar` and `c1` scrapers, resolving the file-not-found proxy load error.
